# SMART-DOOR-LOCK
Readme(important)
install the library first!
u can see what u need in the code(#include)
better not to use arduino ide, try using platform io(so many problems on arduino ide)
pls note that the mqtt must be connected if you want your keypad works!(there is blocking on the code, need a bit change so it can be used even the esp32 is offline)
use esp32 devkitv1(recommended)
OLED 0.96
u can use 5V for servo and OLED
